"""Daily S&P 500 analysis app powered by Gonka + TradingAgents.

Top-level entry points:
    app.runner.run_daily(top_n=...)    — execute one analysis pass
    app.scheduler.start()              — run every day at the configured time
    streamlit run app/dashboard.py     — view stored decisions
"""
