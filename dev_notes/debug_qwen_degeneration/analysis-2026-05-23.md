# Qwen3-235B-FP8 输出退化诊断 (2026-05-23)

## 摘要

生产环境 `Qwen/Qwen3-235B-A22B-Instruct-2507-FP8` 经由 Gonka router 调用时，
~5–7% 的 LLM 调用会进入字节级退化（`#(c(c(c…`、`피피피…`、`/access随着时间…`、
`체부 업 …` 等 token 循环），跑满 `max_tokens` 才停。整个 ticker run 14
个 LLM 调用里典型出 1 次以上，且第一次出现后通过下游 prompt 污染（被退化
agent 写入 state 的报告被下一个 agent 当输入）放大失败率。

**根因（已锁定）**：Gonka 服务端对 **`max_completion_tokens` 取特定数值**
的请求进入了 buggy 解码路径。实测 **256、2048、4000、8000、8192** 都触发；
**4096** 是测试覆盖范围内最稳的值。生产代码 `_GENERATION_DEFAULTS =
{"max_tokens": 8192}` 正好命中坏值。

**次要原因**：Qwen 在填 tool-call args 时把 `curr_date` hallucinate 成
`2226-05-23`（多打了个 2）。tool 不校验日期，返回不相关的新闻，下游 agent
吃到这堆垃圾后更容易触发退化。

**修复**（已写入工作树，未 commit）：
1. Settings UI 新增 per-family `max_tokens` 配置：**Qwen 默认 4096**，
   **Kimi 保留 8192**。详见 [#修复-1-per-family-max_tokens](#修复-1-per-family-max_tokens)。
2. 所有 LLM-决定的 date 参数在 tool 入口 sanitize：未来 > 7 天 → clamp
   到 today；过去 > 3 年 → clamp 到 3 年前 boundary。详见
   [#修复-2-llm-日期参数-sanitize](#修复-2-llm-日期参数-sanitize)。

---

## 1. 初始症状

用户给的 `llm_debug1.jsonl` 是 Research Manager 的一次调用：

- 输入：6903 字符 user message（含完整辩论历史 + 评级 rubric）
- 输出：**33946 字符多语言混合 token 循环**，`finish_reason='length'`，
  168 秒，跑满 max_tokens=8192
- 输出片段：

  ```
  无聊用电 Citizens Citizens Facts papers_cpusmemo.Checked次要 Cooler用电
  麻痹不妨买用电不由 mListeneriske峡用电麻痹spath sens Cooler Facts tim用电
  麻痹麻痹麻痹麻痹yro ... 鸠鸠 ... merit Peace SCREEN 鸠鸠 ... /access/access/access
  Reeves和平 Forget ...
  ```

第二个 jsonl `llm_debug_8parallel_degeneration.jsonl`（生产 max_workers=8
那次 run）里 143 个调用里 8 个明显退化：

| line | ts (UTC)              | agent              | elapsed (ms) | finish | outlen  |
|------|------------------------|--------------------|-------------:|--------|--------:|
| 43   | 13:06:59 | Market Analyst       | 122,131  | length | 11,878  |
| 63   | 13:07:04 | Market Analyst       | 238,472  | length | 16,284  |
| 66   | 13:07:54 | Market Analyst       | 216,007  | length | 23,269  |
| 82   | 13:09:57 | Fundamentals Analyst | 162,222  | length | 17,063  |
| 84   | 13:12:39 | Bull Researcher      | 3,736    | stop   | **14** (`<\|im_start\|>_1`) |
| 86   | 13:08:13 | Fundamentals Analyst | 294,262  | length | 14,643  |
| 102  | 13:12:06 | News Analyst         | 194,365  | length | 20,756  |
| 128  | 13:16:13 | Trader               | 156,566  | length | 8,192   |

正常调用 median elapsed 14 秒，退化调用都是 2–5 分钟 —— 服务端真的在产
8K-23K token 的循环，不是 hang。退化输出的"指纹 token"在多次失败间高度
重叠（`Citizens` / `Facts` / `merit` / `/access` / `鸠` / `麻痹` / `체부`），
显示这是同一个 logit-collapse pattern 的不同投影。

---

## 2. 被否决的几个假说

| 假说 | 判定 | 反例 |
|---|---|---|
| 输入污染：`<\|im_start\|>` 字面值出现在 user 文本里破坏对话模板 | **错** | 把含 `<\|im_start\|>_1` 字符串的 RM input 重发，4/4 正常输出 `**Rating**: Underweight ...` |
| 并发触发：`max_workers=8` 同时打服务端导致 batch 污染 | **错** | 用户切到 max_workers=1 跑 1 个 ticker 仍触发（28 calls 中 2/28 = 7.1%） |
| 节点路由：router 随机分到 unhealthy worker | **错** | `x-devshard-id` 是 sticky 的（同 API key 黏在 shard 2034），同 prompt 重发 100% 复现 |
| 输入 hallucinate 触发：tool 结果里 `2226` 日期把模型搞糊涂 | **部分错** | 修复 `2226→2026`、删 tool 结果都让模型从退化变成"重发 tool_call"，并没真正写出 report |

第一个假说尤其有教训价值：诊断早期我把 `<|im_start|>_1` 当成 root cause
（看起来太像 Qwen chat template token 泄漏了），实际重发实验直接打脸。
**没有重发实验就别下结论**。

---

## 3. 决定性实验：max_completion_tokens 扫描

把 `debug_log_1.json` 完整 prompt（5 messages，~13K char，含 NVDA news +
global news 两个 tool 结果）通过 bare OpenAI SDK 原样发，**仅改
`max_completion_tokens`**：

| `max_completion_tokens` | 模式 | 实际生成 | fingerprint |
|---:|---|---:|---|
| 256  | **BYTE-GARBAGE** | 256  | `피피피피피피피피피피피...` |
| 512  | RE-TOOL-CALL | 46  | content='', 重发 `get_global_news` |
| 1024 | RE-TOOL-CALL | 46  | 同上 |
| 2047 | RE-TOOL-CALL | 46  | 同上 |
| **2048** | **BYTE-GARBAGE** | **2048** | `#(ng(c(c(c(c(c(c)((c(c(c...` |
| 2049 | RE-TOOL-CALL | 46  | 同上 |
| 3000 | RE-TOOL-CALL | 46  | 同上 |
| **4000** | **BYTE-GARBAGE** | **4000** | `피피피피...` 韩语字符 loop |
| **4096** | **TEXT** ⭐ | 155 | `It appears there are issues with the data retrieved. The global news results reference a future date (2226), which is clearly erroneous...` |
| 5000–7000 | RE-TOOL-CALL | 46  | 同上 |
| **8000** | **BYTE-GARBAGE** | **8000** | `<\|im_start\|>: ...Stock/Stock/Stock/Stock/...` |
| 8191 | RE-TOOL-CALL | 53  | 同上 |
| **8192** | **BYTE-GARBAGE** | **8192** | `체부: 부 ... 회사란국부 업 업` (= 生产 line 7 同款) |
| 8193 / 16384 | **BYTE-GARBAGE** | 8192 (clamped) | 同 8192 |

确认事项：

1. **`max_completion_tokens=2048` 是稳定 bug**：边界两侧 2047/2049 都 OK，
   只有 2048 这一个值崩。
2. **`max_completion_tokens=8192` 是稳定 bug**：≥8192 全崩（服务端硬 cap），
   8191 OK。
3. **输出是 deterministic**：同 mct 重跑 4 次结果字节一致 → 服务端在用接近
   greedy 的解码，没有 sampling 多样性。一旦进入坏路径无法自愈。
4. **`max_completion_tokens=4096` 是测试覆盖里唯一产出有意义自然语言的值**。
   而且模型分析的内容（"global news references a future date 2226"）正好
   是次要 bug 的根因 → 模型其实知道 tool 结果有问题，只是没机会说出口。
5. RE-TOOL-CALL 模式（content='' + 模型重发 tool）不是 garbage，但也不是
   成功 —— 模型放弃 ingest 当前 tool 结果，进入死循环。对生产管道来说和
   失败没区别。

### 客户端层面的"隐藏修复"

实测同样输入：
- **LangChain ChatOpenAI（max_tokens=8192）** → 3/3 输出干净的 tool_calls
- **bare OpenAI SDK（max_completion_tokens=2048）** → 4/4 退化

抓 wire body 对比后唯一差别就是 max_completion_tokens 数值。LangChain
当时凑巧用的是 8192（项目硬编码默认），打中了「凑巧不崩」的 prompt+mct
组合 —— 但同一个 8192 在完整 tool-loop prompt 上又崩。结论：**两个客户端
没有真正差异，是 max_completion_tokens 本身的服务端 bug**。

---

## 4. 次要 bug：LLM hallucinate 日期

`debug_log_1.json` 里 News Analyst 的 tool_call args：

```json
{
  "name": "get_global_news",
  "args": {"curr_date": "2226-05-23", "look_back_days": 7, "limit": 10}
}
```

System prompt 明确写了 `current date is 2026-05-23`，但 LLM 在填 args
时多打了一个 `2` → 200 年后的日期。`get_global_news` tool 不校验日期，
照样去查并返回了一堆离题的鞋价 / 咖啡 / 银矿新闻。

这本身不直接导致 byte-level garbage（max=8192 + 没有这个 hallucinate 也
照样崩），但：

- 让 tool 结果污染下游 prompt
- 让模型对 tool 结果"不信任"，倾向于 RE-TOOL-CALL 而不是 ingest → 加剧
  失败率

实测 `get_news` 同次调用里两个 date 参数 `start_date=2026-05-16,
end_date=2026-05-23` 是对的，只有 `get_global_news` 的 `curr_date` 错了。
没有规律 —— 任何 LLM-决定的 date 参数都可能 hallucinate。

---

## 5. 修复

### 修复 1: per-family max_tokens

新增两个 Settings 字段（前后端 + Settings UI），按 model 名 substring
匹配 family 选 max_tokens：

| 文件 | 改动 |
|---|---|
| `app/settings_store.py` | 新常量 `QWEN_DEFAULT_MAX_TOKENS=4096`、`KIMI_DEFAULT_MAX_TOKENS=8192`；`defaults_from_env` / `settings_to_env` / `public_view` 都接通 |
| `app/api.py` | `SettingsUpdate` 加 `qwen_max_tokens` / `kimi_max_tokens` (range 256–32768)；`put_settings` 写回 |
| `tradingagents/llm_clients/gonka_client.py` | 新 `_max_tokens_for(model)`，按 `model.lower()` 含 `qwen` / `kimi` 选对应 env var；`_apply_streaming_defaults` 改用它；旧的 `_GENERATION_DEFAULTS` 静态值删除 |
| `frontend/composables/useApi.ts` | `SettingsResponse` / `SettingsUpdate` 两个 interface 加字段 |
| `frontend/pages/settings.vue` | "Model & runner" section 加两个 number 输入框 |

工作流：

```
Settings UI 改值 → PUT /api/settings → settings.json 写入
  → 下次 run 启动 python -m app.runner subprocess
  → settings_to_env() export TRADINGAGENTS_QWEN_MAX_TOKENS / _KIMI_
  → gonka_client._max_tokens_for(self.model) 读 env
  → LangChain ChatOpenAI 用对应 max_tokens
```

**Kimi 不动 8192** 是因为它是 reasoning 模型，CoT 通过同 SSE 通道流式发
empty `delta.content` chunks，LangChain `generate_from_stream` 在零
content chunks 时会 ValueError，所以 Kimi 反而需要 8192 的 headroom 给
reasoning + visible answer。

**为什么不是 4097 / 4095 之类的"避开 4096 万一也是坏值"**：实测 4096 是
测试覆盖范围里**唯一**产出真实自然语言的值。其他 OK 的值（512/1024/2047/
2049/3000/5000/6000/7000/8191）全部是模型放弃 ingest tool 结果、重发
tool_call，对生产没用。

### 修复 2: LLM 日期参数 sanitize

新文件 `tradingagents/agents/utils/date_sanitize.py`：

```python
def sanitize_llm_date(value, *, tool, arg):
    # YYYY-MM-DD 解析失败 → passthrough (vendor 自己报错)
    # 未来 > 7 天 → clamp 到 today, log WARNING
    # 过去 > 3*365 天 → clamp 到 3 年前 boundary, log WARNING
    # 其他 → 原样
```

容忍 ±7 天未来覆盖 timezone / clock skew；3 年历史窗口保留合理的回测查询
能力。

在 8 个 tool 入口拦截：

| 文件 | tool / arg |
|---|---|
| `news_data_tools.py` | `get_news` (start_date, end_date), `get_global_news` (curr_date) |
| `core_stock_tools.py` | `get_stock_data` (start_date, end_date) |
| `technical_indicators_tools.py` | `get_indicators` (curr_date) |
| `fundamental_data_tools.py` | `get_fundamentals` / `get_balance_sheet` / `get_cashflow` / `get_income_statement` (curr_date) |

不改 tool 签名，对 LLM 透明。命中时 logger.warning，监控用：

```sh
grep "Likely an LLM hallucination" ~/.tradingagents/app/logs/
```

---

## 6. 未解问题 / 上游 bug report

1. **Gonka / vLLM 服务端为什么对 `max_completion_tokens ∈ {256, 2048, 4000,
   8000, 8192}` 进 buggy 路径？** 推测跟 KV-cache slot 对齐 / continuous-
   batching 的某个 fast-path 有关，但没有服务端源码无法确认。值得给 Gonka
   团队上报这个 minimal repro：

   ```python
   client.chat.completions.create(
       model="Qwen/Qwen3-235B-A22B-Instruct-2507-FP8",
       messages=[{"role": "user", "content": "..."}],
       max_completion_tokens=2048,
       stream=True,
   )
   ```

2. **4096 是不是真的所有 prompt 都安全？** 当前只在一个 prompt 上测了。
   生产铺开后需要监控是否有新的坏值组合。建议加监控：
   - `finish_reason == 'length'` 比例
   - 输出 char-3gram dominance > 0.3 的调用比例
   - 含 `<|im_start|>` / `<|im_end|>` 字面值的输出

3. **是否应该加输出退化检测 + 重试？** 修复 1+2 不能保证 100% 治愈
   （它们消除了已知触发点，但 prompt 空间还有未探索的角落）。可以在
   `LLMDebugLogger` 旁边加一个 degeneration detector，命中就抛异常让上层
   LangGraph 重试。但服务端是 greedy 的，同 prompt 重试结果一致 → 重试
   必须改 mct 值或 prompt，单纯 retry 无效。

4. **本次诊断里 142 calls 都靠 router (api.gonkascan.com)**。SDK 直连
   (node4.gonka.ai) 没测。如果 SDK 直连不命中相同 bug，对生产是另一条
   出路（但要权衡延迟、签名 overhead）。

---

## 附录：原始数据 & 复现脚本

同目录文件：

- `debug_log_1.json` — 首个 News Analyst 退化调用的完整 LangChain log，
  导致单线程 run 失败的"零号样本"
- `llm_debug_8parallel_degeneration.jsonl` — max_workers=8 那次 run 的
  完整 LLM 调用日志（143 calls，8 次退化）

`/tmp/` 下临时探测脚本（未推送）：

- `/tmp/replay_log1.py` — 把 debug_log_1.json 原样重发 N 次，确认 100%
  复现
- `/tmp/bisect_log1.py` — 在 prompt 上做变体实验定位 trigger
- `/tmp/probe_qwen_penalty.py` — 多个 sampling 参数 regime（frequency /
  presence penalty / temperature）扫描

## 时间线

- 2026-05-23 13:06–13:20 — 用户原始 `max_workers=8` ticker run（8 个 S&P
  500 ticker），143 calls 中 8 次退化
- 21:24 — 收到 `llm_debug1.jsonl`（RM 退化），开始诊断
- 22:00–22:25 — 单线程 1 ticker 复现，28 calls 中 2 次退化（7.1%）
- 22:30–22:40 — `max_completion_tokens` 数值扫描定位 bug
- 22:40+ — 修复 1+2 写入工作树
